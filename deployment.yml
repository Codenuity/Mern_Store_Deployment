apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodeapp-deployment
  namespace: node-app
  labels:
    app: nodeapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nodeapp
  template:
    metadata:
      labels:
        app: nodeapp 
    spec:
      containers:
      - name: nodeserver
        image: codenuity/nodeapp:26
        ports:
        - containerPort: 3000
